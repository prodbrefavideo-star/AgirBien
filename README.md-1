# AI-Musulmane — AgirBien

Assistant spirituel (React + Vite). Q/A basé sur le Coran, la Sunna et la logique.
Fonctionnalités : moteur d'intention, rappels, export conversation.

## Lancer en local
1. Installer Node.js (https://nodejs.org)
2. Dans le dossier du projet :
   npm install
   npm run dev
3. Ouvrir http://localhost:5173

## Déploiement
Recommandé : GitHub + Vercel (import du repo, build = `npm run build`, output = `dist`)
